# Core HF stack
transformers>=4.43
datasets>=2.18
accelerate>=0.30
evaluate>=0.4.2
huggingface_hub>=0.23
safetensors>=0.4.2
sentencepiece>=0.2.0
tokenizers>=0.15

# Audio / features
torchaudio>=2.3.0
librosa>=0.10.2
soundfile>=0.12.1
audioread>=3.0.1
pyannote.audio>=3.1.1 ; python_version >= "3.10"  # only if you actually use it

# Metrics / text
jiwer>=3.0.3

# Data / utils
numpy>=1.26
scipy>=1.11
pandas>=2.0
pyarrow>=15.0
tqdm>=4.66
rich>=13.7

# Optional—only if your code uses them
bitsandbytes>=0.43 ; platform_system == "Linux"
peft>=0.11
einops>=0.7
wandb>=0.17
